/*.yml
!/stalkerhek.example.yml
!/.travis.yml

/stalkerhek
ChatGPT*.png

# Generated native build artifacts — produced by android/build_apk.sh and CI,
# never committed (engine .so must be built per-ABI from source).
android/app/src/main/assets/stalkerhek*
android/app/src/main/jniLibs/**/*.so
android/app/src/main/jniLibs/**/*.h

# Local tooling artifacts (knowledge graph + Claude Code)
/graphify-out/
/.claude/
/CLAUDE.md
stalkerhek.db
